# UNUSED CLEANUP REPORT

Cleanup date: 2026-06-03
Source audit: `docs/audits/UNUSED_CLEANUP_CANDIDATES.md`
Task: `task/task-003-cleanupunsed.md`

## 1. Summary

Performed safe cleanup only. Removed confirmed low-risk junk and regenerable local artifacts explicitly allowed by the user:

- `.DS_Store` files
- `dist/gg-handoff.zip`
- `test-results/.last-run.json`
- stale files under `qa/audit-output/`
- empty `qa/audit-output` and `test-results` folders after file removal

No medium-risk or high-risk candidates were removed. Production flags were not changed. Validation stopped at `npm ci` because it failed with the same local `esbuild` binary compatibility error found during the architecture audit.

## 2. Removed Files/Folders

| Removed Path | Type | Evidence | Risk | Validation |
|---|---|---|---|---|
| `./.DS_Store` | macOS junk | Found by `find . -name .DS_Store -o -name __MACOSX`; ignored by `.gitignore`. | Low | Removed; follow-up `find` returned no `.DS_Store`. |
| `./tools/.DS_Store` | macOS junk | Found by `find`; ignored by `.gitignore`. | Low | Removed; follow-up `find` returned no `.DS_Store`. |
| `./__gg/.DS_Store` | macOS junk in generated/runtime area | Found by `find`; `__gg/` is generated/runtime asset space per audit. | Low | Removed; follow-up `find` returned no `.DS_Store`. |
| `./__gg/assets/.DS_Store` | macOS junk in generated/runtime area | Found by `find`; ignored by `.gitignore`. | Low | Removed; follow-up `find` returned no `.DS_Store`. |
| `./dist/.DS_Store` | macOS junk in generated output | Found by `find`; `dist/` is generated output. | Low | Removed; follow-up `find` returned no `.DS_Store`. |
| `./.cloudflare-build/public/__gg/assets/.DS_Store` | macOS junk in staging output | Found by `find`; `.cloudflare-build/` is staging output. | Low | Removed; follow-up `find` returned no `.DS_Store`. |
| `./.cloudflare-build/public/assets/.DS_Store` | macOS junk in staging output | Found by `find`; ignored by `.gitignore`. | Low | Removed; follow-up `find` returned no `.DS_Store`. |
| `./.cloudflare-build/public/store/.DS_Store` | macOS junk in staging output | Found by `find`; staging output. | Low | Removed; follow-up `find` returned no `.DS_Store`. |
| `./qa/.DS_Store` | macOS junk | Found by `find`; ignored by `.gitignore`. | Low | Removed; follow-up `find` returned no `.DS_Store`. |
| `./docs/.DS_Store` | macOS junk | Found by `find`; ignored by `.gitignore`. | Low | Removed; follow-up `find` returned no `.DS_Store`. |
| `./pwa/.DS_Store` | macOS junk | Found by `find`; ignored by `.gitignore`. | Low | Removed; follow-up `find` returned no `.DS_Store`. |
| `./task/.DS_Store` | macOS junk | Found by `find`; ignored by `.gitignore`. | Low | Removed; follow-up `find` returned no `.DS_Store`. |
| `./.github/.DS_Store` | macOS junk | Found by `find`; ignored by `.gitignore`. | Low | Removed; follow-up `find` returned no `.DS_Store`. |
| `./views/.DS_Store` | macOS junk | Found by `find`; ignored by `.gitignore`. | Low | Removed; follow-up `find` returned no `.DS_Store`. |
| `./assets/.DS_Store` | macOS junk in public asset tree | Found by `find`; ignored by `.gitignore`. | Low | Removed; follow-up `find` returned no `.DS_Store`. |
| `./store/.DS_Store` | macOS junk in Store output tree | Found by `find`; ignored by `.gitignore`. | Low | Removed; follow-up `find` returned no `.DS_Store`. |
| `./src/.DS_Store` | macOS junk in source tree | Found by `find`; ignored by `.gitignore`. | Low | Removed; follow-up `find` returned no `.DS_Store`. |
| `./src/css/modules/.DS_Store` | macOS junk in source CSS module tree | Found by `find`; ignored by `.gitignore`. | Low | Removed; follow-up `find` returned no `.DS_Store`. |
| `dist/gg-handoff.zip` | Regenerable handoff archive | Audit identified it as generated by `npm run gaga:handoff:pack`; user explicitly allowed deletion. | Low | Removed; `test -e dist/gg-handoff.zip` returned absent. |
| `test-results/.last-run.json` | Local test runner output | Audit identified it as ignored local output; user explicitly allowed deletion. | Low | Removed; no files remained under `test-results`. |
| `qa/audit-output/zip-audit.latest.json` | Stale advisory audit output | Audit identified `qa/audit-output/` as ignored generated/advisory output; user explicitly allowed deletion. | Low | Removed; no files remained under `qa/audit-output`. |
| `qa/audit-output/zip-audit.latest.md` | Stale advisory audit output | Audit identified `qa/audit-output/` as ignored generated/advisory output; user explicitly allowed deletion. | Low | Removed; no files remained under `qa/audit-output`. |
| `qa/audit-output/` | Empty folder | Became empty after removing allowed stale audit output. | Low | Removed with `rmdir`. |
| `test-results/` | Empty folder | Became empty after removing `.last-run.json`. | Low | Removed with `rmdir`. |

## 3. Kept Candidates

| Path | Reason Kept | Risk |
|---|---|---|
| `Archive backup 1.zip` | Audit marked low-to-medium, not low-only. User did not explicitly allow this file. | Low to medium |
| `Archive backup 2.zip` | Audit marked low-to-medium, not low-only. User did not explicitly allow this file. | Low to medium |
| `Archive backup 3.zip` | Audit marked low-to-medium, not low-only. User did not explicitly allow this file. | Low to medium |
| `Archive.zip` | Audit marked low-to-medium, not low-only. User did not explicitly allow this file. | Low to medium |
| `task/gg-95-percent-task-pack-v3-worker-boundary-updated.zip` | Audit marked medium; user requested no medium-risk deletion. | Medium |
| `qa/audit/asset-size.txt` | Audit marked medium. | Medium |
| `qa/audit/first-paint-risk-grep.txt` | Audit marked medium. | Medium |
| `docs/archive/template-deprecated/*` | User explicitly said not to delete; audit marked high risk. | High |
| `task/index-backup.xml` | User explicitly said not to delete; audit marked high risk. | High |

## 4. Not Touched Without Approval

| Path | Reason |
|---|---|
| `index.xml` | Protected Blogger template source; user explicitly said not to delete. |
| `worker.js` | Protected Worker route/policy source; user explicitly said not to delete. |
| `registry/*` | Registry contracts are source of truth; user explicitly said not to delete. |
| `src/js/modules/*` | Controller/module source; user explicitly said not to delete. |
| `store/data/manifest.json` | Generated Store data; user explicitly said not to delete. |
| `dist/store/data/manifest.json` | Generated Store data; user explicitly said not to delete. |
| `flags.json` | Production/development flags; user explicitly said not to change production flags. |

## 5. Commands Run

| Command | Result |
|---|---|
| `sed -n '1,260p' task/task-003-cleanupunsed.md` | Pass |
| `sed -n '1,220p' docs/audits/UNUSED_CLEANUP_CANDIDATES.md` | Pass |
| `git status --short` | Pass |
| `find . -name .DS_Store -o -name __MACOSX` | Initially found `.DS_Store`; after cleanup returned no output. |
| `find qa/audit-output -maxdepth 1 -type f \| sort` | Found stale audit files before cleanup. |
| `rm -f ...` | Pass; removed only allowed files. |
| `rmdir qa/audit-output test-results` | Pass; removed empty folders. |
| `test -e dist/gg-handoff.zip` | Returned absent after cleanup. |
| `test -e task/index-backup.xml` | Returned present after cleanup. |
| `test -d docs/archive/template-deprecated` | Returned present after cleanup. |
| `sed -n '1,45p' flags.json` | Pass; confirmed flags still development and unchanged by cleanup. |
| `npm ci` | Failed; validation stopped here. |

## 6. Failed Commands

| Command | Exit | Failure |
|---|---:|---|
| `npm ci` | 1 | `node_modules/esbuild/bin/esbuild --version` failed with `dyld: Symbol not found: _SecTrustCopyCertificateChain`; binary was built for Mac OS X 12.0 and this local system lacks the expected Security framework symbol. |

Not run because validation stopped after failure:

- `npm run build`
- `npm run ci:qa`
- `npm run ci:85`
- `npm run ci:95`

## 7. Remaining Cleanup Recommendations

1. Resolve the local `npm ci`/`esbuild` compatibility problem before continuing cleanup validation.
2. Review low-to-medium root archive ZIP files with the owner before deletion.
3. Keep medium/high-risk cleanup candidates for a separate approval task.
4. Do not touch production flags until an explicit production-readiness task.
