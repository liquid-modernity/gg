# Extraction Map v1

HTML/XML remains the SSR and hook truth. CSS owns visual/layout/motion/state styling. JS owns behavior/data binding/state/event/focus/gesture. JSON registry is mostly build/edge/lazy data; only JSON-LD and tiny critical config/copy belong in critical HTML.

See `template/partials/` and `src/` for line-preserved extraction.
