# Asset Architecture v1

Blogger upload: one final XML. Cloudflare: hashed CSS/JS, PWA files, `/landing`, `/store`, flags, lazy copy namespaces, and optional cached JSON endpoints. Source remains modular; release remains lean.
