# Legacy donor area

This folder is reference-only. Do not import or bundle directly from here.

The old `src/js/modules/*.fragment.js` files are preserved here because many are not standalone syntax-valid JavaScript.
Use them only as migration notes when extracting logic out of `src/modules/legacy-app/legacy-app.js`.
