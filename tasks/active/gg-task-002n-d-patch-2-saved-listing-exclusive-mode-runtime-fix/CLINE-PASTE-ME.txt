Run TASK-002N-D-PATCH-2 using the prompt at:

ai/agent/prompts/CLINE-SNIPER-TASK-002N-D-PATCH-2-SAVED-LISTING-EXCLUSIVE-MODE-RUNTIME-FIX.md

Important: this is a runtime UX contract fix. Saved mode must become exclusive:
- Native/latest rows must not be visible while Saved mode is active.
- Saved rows must occupy the listing surface as the active mode, not append under Latest/native rows.
- Load-more and pagination must be hidden in Saved mode.
- The visible mode label/dropdown must not still say `Latest` when Saved mode is active. Use `Saved` or hide/neutralize the selector while Saved is active.
- `Details` must not be visible as raw text in saved rows.
- Empty saved state must still use/cloned from template.
- Keep JS template-first and preserve `check:public-dom` with `needsTemplate=0` and `unclassified=0`.

Run the full validation command at the end:

npm run doctor && npm run build && npm run check && npm run check:softcode && npm run check:public-softcode && npm run check:public-ui && npm run check:public-dom && npm run check:legacy-bridge && npm run console:check && npm run studio:check && npm run deploy:dry && bash scripts/task002n-d-patch-2-acceptance.sh
